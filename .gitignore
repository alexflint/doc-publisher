# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

/oauth_credentials.json
/doc-publisher
/scratch/
/.cache/

# Ignore files in out but do not ignore the placeholder, which
# guarantees that we get this dir when we "git clone"
/out/*
!/out/.placeholder

# commit encrypted secrets but nothing else in the secrets dir
**/secrets/*
**/!secrets/*.encrypted
